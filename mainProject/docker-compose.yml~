name: ticketcinema

services:
  backend:
    build:
      context: ./mainBackend/be-src
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    depends_on:
      mysql:
        condition: service_healthy
    container_name: ticketcinema-backend
    restart: no
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://10.1.1.74:3306/ticketdb?useSSL=false&serverTimezone=Asia/Ho_Chi_Minh&allowPublicKeyRetrieval=true  # Thêm allowPublicKeyRetrieval      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: kien123456
      SPRING_JPA_PROPERTIES_HIBERNATE_DIALECT: org.hibernate.dialect.MySQL8Dialect
      SPRING_MAIL_USERNAME: kien0610minh@gmail.com
      SPRING_MAIL_PASSWORD: ivrt wssp oktn vprb

  mysql:
    image: mysql:8.0
    environment:
      - MYSQL_ROOT_PASSWORD=kien123456
      - MYSQL_DATABASE=ticketdb
    ports:
      - "3307:3306"
    volumes:
      - mysql-data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
    container_name: ticketcinema-mysql
    restart: unless-stopped

volumes:
  mysql-data: